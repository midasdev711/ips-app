stages:
  - test

test:
  stage: test
  script:
    - docker-compose build
    - docker-compose run --rb web rake db:create
    - docker-compose run --rm web rspec

after_script:
  - docker-compose down
